/***********************************************************************************
*	Copyright: ©2015 - 2024 Ozan Nurettin Süel (sicherheitsschmiede)                   *
*	License  : Licensed under Apache 2 [https://apache.org/licenses/LICENSE-2.0.txt] *
*	Author   : Ozan Nurettin Süel (Sicherheitsschmiede)										           * 
***********************************************************************************/
module uim.oop.collections.list;

import uim.oop;

// Lists are ordered collections, where users has precise control over where in the list each element is inserted. 
// The user can access elements by their long index (position in the list), and search for elements in the list.
// Lists allow duplicate elements.
/* interface IListx(T = DOOPObject) : ICollection {
	T item(size_t index); 
	O item(this O)(size_t index, T value); 
}

class DListx(T = DOOPObject) : ICollection {
	this() {}
	T item(size_t index) { return null; } 
	O item(this O)(size_t index, T value) { return cast(O)this; } 
} */
// TODO: error with auto List() { return new DList(); }

